import {connect} from 'react-redux'
import ScreenHomeList from '../../Screens/ScreenHomeList'
const mapStateToProps = state =>({
    
})
const mapDispatchToProps ={

}
export default connect(
    mapStateToProps,
    mapDispatchToProps,
)(ScreenHomeList)