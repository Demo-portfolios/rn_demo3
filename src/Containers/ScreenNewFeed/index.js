import {connect} from 'react-redux'
import ScreenNewFeed from '../../Screens/ScreenNewFeed'
const mapStateToProps = state =>({
    
})
const mapDispatchToProps ={

}
export default connect(
    mapStateToProps,
    mapDispatchToProps,
)(ScreenNewFeed)