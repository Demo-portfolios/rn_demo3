import {connect} from 'react-redux'
import ScreenChatPerson from '../../Screens/ScreenChatPerson'
const mapStateToProps = state =>({
    
})
const mapDispatchToProps ={

}
export default connect(
    mapStateToProps,
    mapDispatchToProps,
)(ScreenChatPerson)