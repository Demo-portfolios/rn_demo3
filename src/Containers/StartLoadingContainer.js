import {connect} from 'react-redux'
import ScreenStateLoading from '../Screens/ScreenStartLoading'
const mapStateToProps = state =>({
    
})
const mapDispatchToProps ={

}
export default connect(
    mapStateToProps,
    mapDispatchToProps,
)(ScreenStateLoading)