import {connect} from 'react-redux'
import ScreenCreateGroup from '../../Screens/ScreenCreateGroup'
const mapStateToProps = state =>({
    
})
const mapDispatchToProps ={

}
export default connect(
    mapStateToProps,
    mapDispatchToProps,
)(ScreenCreateGroup)